# AssembleAtEase — Single Prioritized Backlog

The one source of truth for tracked work. Owned by the Product Manager seat (see CLAUDE.md → Backlog Discipline). Priority-ordered. Nothing is left as "maybe later" — every deferral lives here. Capturing an item does **not** mean building it now; the "first 25 jobs" stage gate and the Executive Board decide sequence. Distribution and completion outrank expansion.

**Legend:** `[ ]` open · `[~]` in progress · `[x]` verified complete (shipped + validated). P0 = launch/money/security · P1 = trust/conversion/ops · P2 = polish.

_Last reconciled: 2026-08-22_

---

## Now (top of stack)

- [ ] **P1 · Customer tracking page never mentions an outstanding quote** _(CX, Customer Trust, Product)_ — [track.js](../api/booking/track.js) `getCustomerLabel()` has no branch for `quote_pending_approval` / `quote_authorization_pending` / a pending `card_saved` quote request, so a customer with a live quote sees the generic "Booking received — we will confirm your appointment shortly." The quoted amount, the approval deadline, and the "nothing is charged until you approve" fact are all invisible on `/track`; the emailed approval link is the only place they exist. **Why:** Rule 9 — the customer must never be surprised about price or timing, and an expired approval link with no visible deadline silently loses the job. The owner side of this was fixed 2026-08-26; this is the matching customer surface.
- [ ] **P2 · A mixed cart quietly turns a priced booking into a quote request** _(Product, UX, Payments)_ — in [book.html](../book.html) `isQuoteFlow` is true when ANY custom-quote item is in the cart, so adding "Custom quote" beside priced items converts the whole cart to an unpriced quote request: `total_price` is stored as the priced-items estimate, nothing is authorized, and the customer is told a quote is coming. The owner dashboard now labels that estimate honestly, but the customer is never told at checkout that their priced selections are no longer a bookable price. Decide the intended behaviour (split the cart, warn at checkout, or keep it) rather than leaving it implicit. **Why:** it is the root cause of "the quote came in as a booking with a price."
- [ ] **P1 · Extend the payment-retry guard to promo and AssembleCash carts** _(Payments, Ops, Engineering)_ — the duplicate-booking guard in [_duplicate-booking-guard.js](../api/booking/_duplicate-booking-guard.js) deliberately skips carts carrying a promo code or an AssembleCash token, so a declined card on one of those still creates a second pending booking. Those carts hold a redemption reservation bound to the original booking ref; re-pointing it at a reused booking is a money change that needs its own audit, not a dedupe. **Why:** the common case is fixed, but a discounted cart can still duplicate — and duplicates are what put three unpaid bookings and three references in front of the owner in the first place.
- [ ] **P2 · Sweep abandoned unpaid bookings automatically** _(Ops, Owner visibility)_ — a booking whose card never authorized sits `pending`/unpaid forever until the owner notices and cancels it by hand. The retry guard stops new ones from piling up, but nothing retires the ones already there or those left when a customer simply closes the tab. A scheduled sweep (or a filtered owner view) should retire them without an email, the same way [cancel.js](../api/booking/cancel.js) now stays quiet for bookings the customer was never told about. **Why:** the owner's queue should show real work, not abandoned checkouts.
- [~] **P1 · Repair Google Business Profile relevance + conversion tracking** _(Growth, SEO, CX, Owner action)_ — **SUBMITTED 2026-08-15:** inaccurate `Furniture store` category pending removal; retired drywall/home-repair services removed; six real service labels added; multi-service description, Facebook, LinkedIn, five authentic job photos, Buda/Kyle service areas, and the tagged `/book?utm_source=google&utm_medium=organic&utm_campaign=gbp` action saved. Google accepted the editor changes; moderation/propagation remains pending. **Why:** the profile produced only 8 interactions (4 calls, 4 website clicks, 0 bookings) from March–August 2026, so accurate relevance and conversion attribution must be established before judging performance.
- [~] **P1 · Build a fresh Google review + response engine** _(CX, Growth, Trust, Owner action)_ — **LIVE WORK COMPLETE / PUBLICATION PENDING 2026-08-15:** individualized owner replies were submitted for all 10 public reviews. Existing automation already sends every eligible completed customer the same secure review path on days 2/5/9, stops after a review, suppresses open cases/return visits, and offers the same optional Google-review link after every rating (no incentives or gating). The website's stale 11-review claim and no-longer-visible review were corrected locally to the verified 5.0/10 truth; deploy and Google reply moderation remain before `[x]`.
- [~] **P1 · Restore owner Search Console access + weekly SEO scorecard** _(SEO, Data, Owner action)_ — **ACCESS CONFIRMED 2026-08-15:** the owning account is `tg703664@gmail.com` (not the Business Profile account). Current 3-month baseline: 97 clicks / 21,838 impressions / 0.4% CTR / average position 39.8; 340 indexed / 34 not indexed; one successful `/sitemap.xml` with 410 discovered URLs; no manual actions or security issues. `/business`, `/locations`, and three priority Austin guides were added to Google's priority crawl queue. **LOCAL SEO FOLLOW-UP READY:** the impression-bearing Austin furniture page and IKEA cost guide now use catalog-backed pricing, reciprocal internal links, and stronger search metadata; the homepage coverage section links to the Austin landing page without changing statewide quick-book behavior. Remaining: deploy the focused SEO files, request recrawl for those two URLs, record this scorecard weekly, and verify the five queued URLs enter the index.
- [ ] **P1 · Earn legitimate authority + local proof in priority Texas markets** _(CEO, Partnerships, SEO, Growth)_ — stop expanding the 324-page city/service inventory until existing pages earn results. Start with Austin/Central Texas, then one proven metro at a time; secure real links/citations from apartment managers, realtors, movers, chambers, suppliers, and completed-job partners, and add market-specific job photos/reviews only after real work exists. Never buy links or create duplicate profiles. **Why:** Search Console confirms only 2 external links (MapQuest and Yahoo), while the site has 21,838 impressions but only 0.4% CTR / average position 39.8 over the latest three months. Technical SEO passes; authority and real local proof are now the limiting factors.
- [~] **P1 · Repair the existing Google Ads campaign + conversion measurement** _(Growth, Brand, CX, Finance, Data)_ — **LIVE REPAIR APPLIED 2026-08-22:** no new campaign was created. `AssembleAtEase ads` remains enabled and Eligible at $9/day on Maximize Clicks; broad-match automation and AI expansion remain off. Added Austin city to the 17 existing ZIP targets, eight controlled phrase-match service-intent keywords (four Furniture Assembly, four TV Mounting), and 22 campaign-level negatives covering DIY, jobs, repairs, parts, rentals, and informational searches. Campaign now has 54 enabled keywords, 28 negatives, and 18 locations. All seven ads remain Eligible; billing is current; no ad is disapproved. The live booking page already contains the correct Stripe-confirmed conversion event for `AW-16551666395/7KS0CIjz1aMcENvFudQ9`; Google shows no webpage/event history because the campaign has produced no ad clicks or bookings, while the shared audit browser also blocks Google collection requests. Keep Maximize Clicks until verified booking conversions provide enough signal; review search terms and delivery after seven days, and consider Maximize Conversions only after conversion recording is proven and there is meaningful conversion history. The optional beta financial-products certification is not a disapproval and should not be started. Remaining: correct the client/agency advertiser relationship, monitor new search terms, move the misplaced TV-mounting broad keyword out of Furniture Assembly, and improve the four Poor existing ads after delivery data exists. **Why:** the repair restores controlled auction reach without increasing spend or giving Google uncontrolled broad-match traffic.
- [ ] **P2 · Remove Austin-only footer from Easer account-status email** _(Easer, Brand, External copy)_ — [api/assembler/update.js](../api/assembler/update.js) still renders `AssembleAtEase · Austin, TX` even though the platform and transactional email standard are statewide. **Why:** the broader review/email consistency test fails on this drift; fix separately without mixing Easer-account copy into the SEO batch.
- [ ] **P2 · Easer deactivation — close remaining edges** _(Security, Ops, Payments, Trust)_ — the reversible Deactivate/Reactivate flow is shipped and safe, with a durable audit log of the reason (owner-visible timeline). Follow-ups: (a) **instant session revocation** — on deactivate, kick a currently-open Easer session immediately instead of letting an existing access token last up to ~1 token lifetime (functionally low-risk since readiness already excludes them, but not instant); (b) **unpaid-earnings heads-up** — warn "owed $X" when deactivating an Easer with unpaid completed-job earnings so a payout isn't forgotten (the archive path checks this; deactivate does not); (c) **re-application guard** — confirm a deactivated person can't silently re-apply with the same email and reset their own status. **Why:** the core feature is complete; these are minor security/finance/abuse edges, not blockers.
- [~] **P1 · Same-day flags are SYNCED (verified) — harden to a single source of truth** _(Payments, QA)_ — **VERIFIED 2026-08-12:** both flags ON together — `SAME_DAY_ENABLED=true` in Vercel Production (owner-confirmed) **and** `sameDay.enabled:true` in [booking-source-of-truth.js](../assets/js/booking-source-of-truth.js); migration 061 columns confirmed present in prod via read-only check. The **server is authoritative on the CHARGE** — [booking.js](../api/booking.js) calls `sameDayFeeForAppointment()` which checks the env gate, so the front-end flag only drives the customer PREVIEW + slot lead-time. A drift can therefore only mis-**display**, never mis-**charge** (Copilot's "sees one total, charged another" is bounded to display, and today they're aligned). **Remaining (real fragility, not a live bug):** two flags must be kept in sync by hand. Proper fix = front-end reads the same-day config FROM the server (one source) so preview can never drift from charge. Build on a preview branch + click-test. _Reframed from Copilot's P0 after verifying the live env it could not see._
- [x] **P0 · Easer session identity is bound to rendered private state** _(Security, Easer, Engineering)_ — shared user-ID binding, immediate private-DOM/cache scrubbing, allowlisted profile bootstrap, and abortable private reads are shipped and Production-verified across all four routes. _Commit `801bd16b`, 2026-08-05._
- [x] **P0 · Authenticated assignments are privately cache-controlled** _(Security, Customer, Easer)_ — the API and every Home/Jobs assignment read use `private, no-store`; authenticated Production response verified. _Commit `801bd16b`, 2026-08-05._
- [x] **P1 · Easer Home stale snapshots and serial load waterfall removed** _(Easer, Ops, UX, Engineering)_ — full assignment snapshots are removed and erased from existing sessions, readiness is deduplicated, and independent earnings load concurrently; Production mobile metrics settle correctly. _Commit `801bd16b`, 2026-08-05._
- [x] **P1 · Easer Jobs resume refreshes and serial earnings loads removed** _(Easer, Ops, Engineering)_ — one identity-bound refresh path handles initial load, resume, reconnect, polling, and offer deep links while assignments and earnings load concurrently. _Commit `801bd16b`, 2026-08-05._
- [x] **P1 · Easer Earnings payout-mode flash and mobile management fixed** _(Easer, Finance, UX)_ — neutral loading gates mode truth, routine status reads avoid Stripe reconciliation, and payout management uses same-tab mobile navigation; Production manual card stays hidden. _Commit `801bd16b`, 2026-08-05._
- [x] **P1 · Easer Profile loading, wake refresh, and external copy stabilized** _(Easer, UX, Engineering)_ — neutral placeholders, concurrent truth reads, GET membership status, wake refresh, and professional review/closure copy are live. _Commit `801bd16b`, 2026-08-05._
- [x] **P1 · Easer Profile update response minimized** _(Security, Easer, Engineering)_ — migration 059 returns only eight Easer-facing fields, all browser writes use it, and migration 060 removes authenticated access to the legacy full-row response. _Production catalog and authenticated RPC behavior verified 2026-08-05._
- [x] **P1 · Easer Profile password recovery repaired** _(Easer, Security, Support)_ — reset links target the canonical `/auth/set-password` flow; mobile behavior and Production source verified. _Commit `801bd16b`, 2026-08-05._
- [x] **P1 · Easer Profile identity and contact-completion states clarified** _(Easer, Ops, UX)_ — contact completion is separate from job readiness and identity guidance shows the correct next action. _Commit `801bd16b`, 2026-08-05._
- [x] **P1 · Easer Profile photo save state hardened** _(Easer, UX, Reliability)_ — avatars are reduced to 384px with a strict encoded-size cap, confirmed save truth rerenders the page, and failed saves restore the persisted avatar and completion state. _Commit `801bd16b`, 2026-08-05._
- [x] **P2 · Easer Profile form and closure accessibility completed** _(Easer, Accessibility, UX)_ — semantic alerts/errors, visible keyboard focus, correct edit focus, and closure-dialog semantics, Escape handling, focus containment, and focus return are shipped and mobile-verified. _Commit `801bd16b`, 2026-08-05._
- [x] **P1 · Easer routes now show a stable non-private loading shell** _(Easer, UX, Reliability)_ — all four routes reveal private state only after identity binding, and the Easer critical-asset version is advanced; Production mobile verification passed. _Commit `801bd16b`, 2026-08-05._
- [x] **P2 · Easer service-worker shell/cache repaired** _(Easer, PWA, Engineering)_ — cache v7 uses current Easer assets, network-first versioned scripts/styles, and a dedicated non-private offline screen; Production source verified. _Commit `801bd16b`, 2026-08-05._
- [x] **P1 · Payout-reminder now confirms live Stripe before nagging** _(Easer, Payments, Trust)_ — the "Set up your payouts" campaign was emailing Easers who had already finished Stripe Connect setup, because it trusted a stale cached `stripe_connect_payouts_enabled` flag. Fix: the reminder cron re-checks the LIVE Stripe account before each send and self-heals the cached flag (new `refreshConnectPayoutState` in `_stripe-connect.js`, `confirmStillIncomplete` on the payout rule, wired into `easer-announcements` cron). No money/payout logic touched. **Shipped commit `dbe35bfe`, 2026-08-11.**
- [x] **P1 · Market-demand form bot barrier (honeypot + timing)** _(Trust & Safety, Data, Marketing)_ — spam bots posted gibberish market requests (fake names/cities), polluting demand data + HubSpot and risking email sender reputation via confirmation emails to spam-trap addresses. Fix: honeypot decoy field + submit-timing guard silently drop bots before any DB write/email/CRM, across `index.html`/`locations.html`/`book.html` forms + `api/market-demand.js`. No CAPTCHA (avoids CSP/dependency change) — held in reserve. **CODE SHIPPED commit `5d205669`; spam-row cleanup VERIFIED done in prod 2026-08-12** (both gibberish `MR-` rows now `status=closed`, only the legit Austin test remains `new`).
- [~] **P1 · Easer tier program "The Pro Path" + reliability floor** _(Supply/Easer Growth, Trust & Safety, Service Quality, UX)_ — full engine + UI shipped: 3 tiers (Starter Pro / Professional / Elite Pro) gated on jobs + rating + acceptance + **completion**, 30-day grace demotion, promotion/grace/demotion emails, customer badge on `/track`, Easer "Pro Path" progress meter (incl. Reliability), and profile IA cleanup (standardized tier naming, Lifetime-earned label, Account&Security consolidation, shrunk Close-account, bottom-nav safe-area). **Reliability floor** closes the gap vs DoorDash completion-rate / Uber cancellation-rate / TaskRabbit reliability-rate: `completion_rate` from real `no_show_flagged` events as the 4th gate; **owner-in-the-loop** alerts on repeat no-shows / chronic decliners (never silent auto-suspend at launch); Easer coaching email. Activates by DATA (no flag). Spec: [easer-tier-program.md](easer-tier-program.md). **Code shipped + migrations 062 + 063 VERIFIED APPLIED in prod 2026-08-13** (ledger rows present; all 7 profile columns confirmed via read-only probe). Remaining before `[x]`: observe one live `tier-check` cron run (07:00 UTC) promote/demote + write `completion_rate` without error. _Commit `14322ee3` + reliability batch, 2026-08-12._
- [ ] **P1 · Surface tier + reliability on the OWNER dashboard** _(Ops, Supply, Data)_ — the owner currently learns of tier changes and reliability flags only via alert emails. Add an Easer roster view: tier, acceptance %, completion %, no-show count, and one-click suspend/reinstate. Makes the "owner decides" enforcement visible without database access (Rule 8).
- [ ] **P2 · Reschedule doesn't proactively email the assigned Easer** _(Easer, Ops)_ — when a customer reschedules, the job is unassigned (`assembler_accepted_at=null`) and the OWNER is told to reconfirm/reassign, but the Easer who had it on their calendar gets no direct "your job moved" email. Routes through the owner today (functional), but a direct Easer notice would prevent a stale calendar entry. Mirror the `easer_job_cancelled` pattern in [reschedule.js](../api/booking/reschedule.js).
- [ ] **P2 · Email sender avatar shows a red "A" in Gmail (BIMI)** _(Brand, DevOps, Owner action)_ — email BODIES now carry the AAE logo, but the inbox-list sender avatar is Gmail's auto-generated red letter, not our logo. Fix is infra, not code: publish a **BIMI** DNS TXT record pointing to an SVG of the AAE mark (needs DMARC at enforcement; a VMC cert makes it show in more clients), or set a profile photo on the sending Google Workspace identity. Owner/DNS task.
- [ ] **P2 · Consistent "suspended" state across all Easer pages** _(Easer, UX)_ — the Home dashboard now shows a clear suspended banner and no longer loops, but Jobs/Profile/Earnings just surface a raw 403 error for a suspended Easer (no loop — they only redirect on 401). Give them the same friendly "Your account is suspended — contact support" state using the `accountStatus`/`suspended` fields now returned by `/api/assembler/readiness`.
- [x] **P2 · Suppress review requests while a damage claim / dispute is open** _(Trust & Safety, CX)_ — SHIPPED: the auto cron now skips any booking with an OPEN `operations_case` (damage/dispute/payment/safety/quality); the owner can still send manually (that path overrides the suppression). Shared `_review-email.js` builder; open-case query verified in prod. _Commit `d43da2cb`, 2026-08-13._
- [ ] **P2 · Owner runbook: genuinely photo-less completion** _(Ops, Service Quality)_ — completion requires a photo (a deliberate proof-before-capture money gate). If no photo can ever be taken, the Easer can't complete AND the owner can't complete a Stripe booking either — the escape hatch is the owner uploading a photo on the Easer's behalf (`owner/upload-completion-evidence.js`) or handling it as owner-manual. Document this as an ops step so a job never gets stuck silently. Not a code change.
- [ ] **P2 · Real Reviews module on Easer profile** _(Easer, Trust, Data)_ — once review data exists, replace the empty state with the aggregate rating + latest 2–3 reviews + "View all reviews". The dash/empty-state is correct until then.
- [ ] **P2 · Escalate market-demand spam defense if it persists** _(Trust & Safety)_ — if honeypot+timing doesn't stop it (e.g. direct-API bots that never load the form), add Cloudflare Turnstile (needs CSP update) or a signed form token. Only if there's evidence of continued spam after the current fix ships.
- [ ] **P2 · Market Demand dashboard: flag/filter suspected spam** _(Data, Ops)_ — give the owner a one-click "mark spam/closed" and a filter to hide junk so the demand signal stays trustworthy for expansion decisions.
- [~] **P1 · Distribution — work the move-in / referral channel for 30 days** _(CEO, VP Marketing)_
  Demand is the real bottleneck, not code. Kit is built (see Done). **Owner action:** approach top-2 partners (apartment/property managers, realtors), leave 5–10 QR counter-cards each, and track completed bookings with `utm_source=partner`. Double down on whichever partner drives the first jobs. This is where the first 25 jobs come from.

## Mobile (P1 — 90% of traffic)

- [x] **P1 · Booking-flow audit (external, verified) — friction/UX pass** _(UX, CPO, Growth)_ — audit rated the flow 8/10. **SHIPPED to production:** removed 4 duplicate "rush/same-day" add-ons (double-charge risk); "the server confirms…"→"we'll verify…"; slot badge "Recommended"→"Earliest available"; tightened cancellation line; per-service running subtotal; **address-first schedule** (ZIP before date/time); **AssembleCash → secondary link**; **consent reworded** (no longer alarming on new furniture, still binds the Terms); **category-card screen removed** (opens straight into the item browser with pills). Plus the **same-day flag hardened to a single source of truth** (server-driven via `/api/booking-config`). **Board decisions on the remainder:**
  - **Notes consolidation — DEFERRED** _(CTO/QA)_: high-risk item-selector restructure with multiple edge cases (custom-quote/notes-only services), saves one click = low value. Not worth the sacred-path risk now.
  - **Conditional consent, full logic — DEFERRED** _(General Counsel)_: the reword already resolved the customer-facing concern; the full "show only for used/reassembly jobs" detection touches mandatory legal consent and is a careful legal-design task, not a bug.
  - **Rename "Service call fee" — KEPT** _(CX/Design)_: low value; the name is referenced in policy, receipt, and owner dashboard — renaming a $5 line for marginal clarity isn't worth the consistency risk at this stage.
- [ ] **P2 · "server" dev-jargon sweep** _(UX)_ — same language on apply.html ("The server currently confirms no application fee"). Replace platform-internal wording with customer/Easer language sitewide.
- [ ] **P2 · Confirm TX taxability of the service-call + same-day fees with a CPA** _(VP Finance, General Counsel)_ — code taxes the $5 service-call fee (and $69 same-day) inside the taxable subtotal; internally consistent and likely correct (labor on TPP), but get the per-service ruling.


- [ ] **P1 · Homepage "double-talk": value props repeated 4–7× ** _(UX, Growth, CPO)_ — "Upfront pricing", "reviewed local pros", and "pay after completion" each recur across the hero, the "Why choose" band, trust items, every service card, how-it-works, and FAQ (grep-confirmed on `index.html`). On mobile you scroll the same three promises repeatedly across 9+ sections → the "double-talk / gotta-scroll / not friendly" the owner reported. Violates the existing no-repeated-value-props rule. Fix = **subtraction**: each benefit once; shorten the mobile homepage so the service tiles / book CTA arrive sooner. Highest-traffic page — plan the cut carefully, don't blind-edit.
- [ ] **P2 · Homepage load-in animation timing** _(UX)_ — service cards use staggered `mCardIn` fade (`animation:...both`); on slow phones content can read as "not there yet" briefly. Verify on a real mid-tier device; consider trimming stagger.
- [ ] **P1 · Horizontal-swipe carousels hide content on mobile** _(UX, Growth)_ — confirmed on real 390px renders: the **pricing "service menu"** (6 service categories) and the **homepage reviews strip** are sideways-swipe carousels. Users routinely don't discover off-screen cards → this is the owner's "can't see certain things, gotta scroll." Dots + a peeking next-card help but many still miss it. Fix: on mobile prefer stacked/vertical or make the swipe affordance unmistakable.
- [ ] **P2 · Cookie banner covers bottom CTAs on first visit** _(UX)_ — `cookie-banner` is `position:fixed;bottom:0;z-index:9999`; on pages where a primary CTA sits at the bottom (e.g., pricing "Book Furniture →") it's obscured until dismissed. Dismissable/standard, but consider lifting page bottom-padding while the banner is shown.
- [ ] **P1 · Booking: mobile scroll-reduction + reachable CTA** _(UX, CPO, CX — owner-directed)_ — device-tested full flow at 390px. Two root causes found (sticky experiment built + reverted to keep checkout safe):
  1. **Empty void** below the CTA on every step = the booking wrapper's `min-height:100vh` ([book.html:45](../book.html#L45)) padding out short steps.
  2. **`position:sticky` for `.step-actions` is BROKEN** by an ancestor `overflow-x:hidden` (the horizontal-overflow guard makes an ancestor the sticky scroll-container). Confirmed: even a hardcoded sticky offset would not pin.
  **Correct fix:** use `position:fixed` bottom action bar (unaffected by the overflow ancestor) + per-panel bottom **clearance** (the `--mobile-actions-clearance` machinery already exists, currently only on item panes — extend to all `.step-panel`) + coordinate `bottom` with the fixed cookie banner (z-9999) via a CSS var. The item panes' existing clearance shows this bar was the original design intent.
  Owner also wants **minimal up/down scroll per step** — "everything in one, or slide to the next step after each input." Phase 2: tighten/collapse per-step content (e.g., collapse the optional AssembleCash block) and/or split long steps into auto-advancing micro-steps so each screen ≈ one viewport. Do carefully on the money path with 390px before/after renders.
- [ ] **P2 · Verify booking validation-error timing** _(QA, UX)_ — on the "Your info" step, Required/email/phone errors appeared on arrival in test (likely a seed artifact). Confirm on a real device that field errors only show after interaction/submit, never on first view.
- [ ] **P2 · Section-picker dual action ambiguity** _(UX)_ — the item-section screen offers both per-card "Open" and a global "Continue to items →"; mild "which do I tap" ambiguity.
- [ ] **P2 · Booking step-numbering is fragile** _(Eng hygiene)_ — internal steps 1/3/4/5 map to "Step 1–4 of 4" with a dead step-2; works but easy to break in maintenance (matches the earlier step-cruft note).
- [ ] **P2 · Service-page hero over-uses the city** _(UX/SEO)_ — e.g. furniture page hero shows "Austin" 3×: photo badge "Completed Austin project" + eyebrow + H1. Trim to keep it within the city-neutral rule (eyebrow/service-area/FAQ).
- [ ] **P2 · Pricing copy brushes the tools-only rule** _(Legal/Trust)_ — "Parts, mounts, and hardware are separate unless listed" implies listed hardware add-ons exist; reconcile with the tools-only positioning / open hardware-add-on decision.
- [ ] **P1 · Authed Easer + Owner mobile UNTESTED** _(QA, Ops)_ — both are auth-gated so headless renders came back blank; the two field-critical mobile surfaces have NOT been visually verified. Test on a real logged-in phone (owner dashboard tables + Easer my-assignments, per Rule #5).
- [ ] **P2 · Verify Easer login + my-assignments responsiveness** _(UX)_ — `assembler/index.html` (login) has 0 media queries; `my-assignments.html` has 1. Spot-check on a real phone.
- _Verified NOT broken (device-tested at true 390px via Edge iframe): no horizontal overflow / clipping on home, booking, or track; booking step-1 tiles + prices render clean. Earlier "severe overflow" alarm was a headless tooling artifact (Edge floored `--window-size=390` at 492px), now corrected._

## Trust & communication (P1)

- [ ] **P1 · SMS notifications + consent** _(CX, Ops, Security)_
  Zero SMS in 200 routes; home-services customers live in texts ("your pro is on the way"). Stage-appropriate: add an SMS-consent checkbox on booking **now** (so no re-consent later); owner texts manually at "en route" for the first jobs; wire a provider post-validation.
- [ ] **P1 · Customer-facing "en route" status on /track** _(CX)_
  `/track` is manual-refresh, no ETA. Owner/Easer-driven status the customer can see + the SMS above closes most of the anxiety gap without realtime infra.
- [ ] **P1 · Step-level booking funnel analytics** _(Data, PM)_
  Can't currently prove where people drop across the 4 booking steps. Add GA4 step events **before** touching the flow. Optimize with data, not vibes.

## Polish & positioning (P2)

- [ ] **P2 · Migrate service generator to shared city list** _(CTO / SEO)_ — `generate-location-pages.js` still holds its own inline `CITIES`/`STATEWIDE_MARKETS`; the new `scripts/lib/texas-cities.mjs` is now the source of truth (recruitment generator uses it). Point the service generator at it too and delete the inline copy so there's ONE definition. **Why:** avoid the two lists drifting apart (Rule 2 — no duplicate truth). Verify by regenerating and confirming an empty diff on the 324 service pages.
- [ ] **P2 · Refresh JobPosting freshness on a cadence** _(SEO)_ — recruitment pages carry a fixed `datePosted`/`validThrough` (~90 days). Re-run `node scripts/generate-easer-recruit-pages.js` periodically (or wire a cron) so Google Jobs postings don't expire. **Why:** stale JobPosting drops out of Google Jobs.
- [ ] **P2 · SMS notifications (booking confirm / dispatch offer / arrival ETA)** _(Ops / DevOps)_ — real trust + ops value. The work is **US A2P 10DLC brand+campaign registration** (~1–2 wks, carrier fees) + a provider pick — lean Twilio for integration simplicity at this stage, NOT an AWS migration. **Why:** SMS beats email for time-sensitive dispatch/arrival. Keep email on Resend — do NOT migrate email to AWS SES (premature scaling, zero stage benefit).
- [ ] **P2 · Easer/owner app-shell blue re-skin** _(Brand & Design-System)_ — `brand-color-lint` reports ~34 non-canonical blues (`#0369A1/#075985/#155E75/#001F2B`…) in the Easer/owner apps (Tailwind palette, not brand tokens). Unify onto brand sky-blue tokens; contrast-sensitive, so its own isolated pass. **Why:** the two app halves don't look like the same brand as the marketing site.
- [ ] **P2 · Hero trust trio** _(Growth)_ — "Upfront price · Pay after completion · Vetted local pro" as a 3-item row; that trio is the wedge vs Thumbtack/Angi.
- [ ] **P2 · Completion after-photos in the customer email** _(CX)_ — evidence exists (`_completion-evidence.js`); send it to the customer as a referral trigger.
- [ ] **P2 · Reviews → meta title + `aggregateRating` JSON-LD** _(Growth)_ — real reviews exist; surface the star count for SEO/CTR.
- [ ] **P2 · Booking step-numbering cruft cleanup** _(VP Engineering)_ — dead comments ("Step 2 removed", "Step 5 rendered") vs live "of 4"; hygiene, not customer-facing.

## Done (verified)

- [x] **P1 · Statewide Easer recruitment engine shipped** — hub `/become-an-easer` + a localized `/easer-jobs-<city>-tx` page for all 54 TX cities, generated by `scripts/generate-easer-recruit-pages.js` off the shared `scripts/lib/texas-cities.mjs`. Honest `JobPosting` JSON-LD (CONTRACTOR, paid per job, no fake salary); served cities = "actively booking", expansion = "get approved to be first in line". Funnel: apply pre-fills `?city=`, homepage band links the hub (direct `/assembler/apply` entry points untouched — smoke-required). 55 sitemap entries. Governance 427/427, smoke, brand-lint all PASS. _Shipped 59083d41, 2026-08-06._
- [x] **P1 · Homepage + marketing teal killed → sky-blue everywhere** — root cause was the desktop `.btn-cyan` `!important` override (teal `#12a4c7→#0a8fb1`) while mobile was already sky, plus `.page-hero`/`.blog-hero` gradients passing through teal and the Easer-CTA turquoise `#4dd0e1`. Converted all to the brand sky ramp at matched lightness (WCAG preserved); desktop button now matches mobile. Semantic success greens intentionally kept. _Shipped 11f553b6; smoke PASS, lint PASS._
- [x] **P1 · Brand color-drift governance guard added** — `scripts/brand-color-lint.mjs` (`governance:color` + in `test:launch`) hard-fails if a brand token drifts out of the blue hue band (catches the recurring teal-green). Corrected wrong `#0094C6` → `#0099CC` on 5 marketing pages + 1 email. **Accessibility note:** book.html's `#0077a8` accent is intentionally allowed (sky-blue too bright for AA accent text on white). _Shipped 86516f33._

- [x] **P0 · Stripe Connect inaccessible-account recovery fixed** — Stripe public support details and Express branding were completed; commit `a9d56984` recognizes revoked/inaccessible connected accounts as recoverable. Production self-healed the stale Easer state, created one correctly configured live Express account, and generated the branded onboarding link. The owner intentionally stopped before entering personal/bank details, so onboarding remains incomplete and no payout moved. Full launch regression and live status checks passed. _2026-08-04._
- [x] **P0 · Easer announcement tables locked down** — migration 058 enabled RLS, revoked `anon`/`authenticated`, preserved `service_role`, and recorded schema state 57–58. Production zero-row probes return HTTP 401 publicly and HTTP 200 through the server credential. _2026-08-04._
- [x] **P1 · Mobile — homepage pinch-zoom re-enabled** — removed `maximum-scale=1` from `index.html` viewport (was the only page of 339 blocking zoom; WCAG 1.4.4). _2026-08-02._
- [x] **P2 · Mobile — waitlist input iOS-zoom fixed** — added `.waitlist-input` to the mobile 16px guard ([marketing.css:224](marketing.css#L224)). _2026-08-02._
- [~] **Corrected (no change needed) · Easer field-input iOS zoom** — false alarm: `easer.css` already forces `input`/`select`/`textarea` to 16px under `@media (max-width:899px)` (lines 816–821), and all Easer pages load easer.css. No fix applied.
- [x] **P1 · Partner outreach kit + move-in QR counter-card built** — pitch, target list, and email/DM templates at [business-artifacts/partner-outreach-kit.md](partner-outreach-kit.md); print-ready 5×7 counter-card with a real scannable QR (segno v5) → `/book?bundle=move-in-ready&utm_source=partner`, published as a private artifact. Enables the distribution item above. _2026-08-02._
- [x] **P1 · Booking — card billing-address AVS false declines fixed** — removed the service `address` block from Stripe `billing_details` in both checkout paths ([book.html:7394](../book.html#L7394) quote/scheduled + immediate payment); billing_details now carries name/email/phone only. Service address still sent to the booking API. Stripe Radar + manual capture still protect. _Shipped 2026-08-02; surgical 2-block diff, smoke PASS, governance 371/371._
- [x] **P1 · Homepage re-assembly guarantee surfaced** — "Assembled right, or we come back free." added to desktop + mobile hero. _Shipped ff5d0e3f; governance 371/371, smoke PASS._
- [x] **P1 · Hero teal drift fixed** — mint-green `#5eead4` pulse dot → sky-blue `#8fe8ff`. _Shipped ff5d0e3f; verified 0 remaining `#5eead4`._
- [x] **P2 · LinkedIn in Organization JSON-LD `sameAs`** — added to homepage + About. _Shipped ff5d0e3f._
- [x] **P2 · Footer social logo icons (Facebook + LinkedIn)** — governed footer across 356 pages. _Shipped eb86f36b._
